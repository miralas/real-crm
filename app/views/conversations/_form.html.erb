<%= form_for @message , url: account_message_path(current_user.account_id, @message), remote: true do |f| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <li class='new' id='message_content'>
    <%= link_to @interlocutor.email, account_user_path(current_user.account, @interlocutor)%>
    <%= f.label "Введите Ваше сообщение" %>
    <%= f.text_area :message %>
    <%= f.submit 'Отправить' %>
  </li>

<% end %>